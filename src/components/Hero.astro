---
import Logo from "./Logo.astro";
---

<section
  class="min-h-screen bg-gradient-to-br from-primary-50 via-accent-50 to-neutral-50 p-8 flex items-center justify-center relative overflow-hidden"
>
  <!-- Decorative background elements -->
  <div class="absolute inset-0 opacity-10">
    <div
      class="absolute top-20 left-10 w-72 h-72 bg-primary-400 rounded-full blur-3xl"
    >
    </div>
    <div
      class="absolute bottom-20 right-10 w-96 h-96 bg-accent-400 rounded-full blur-3xl"
    >
    </div>
  </div>

  <div class="max-w-5xl mx-auto text-center relative z-10">
    <div class="mb-8">
      <div class="flex items-center justify-center gap-4 mb-8">
        <Logo class="w-20 h-20 drop-shadow-lg" />
        <h1
          class="text-display-1 font-display bg-gradient-to-r from-primary-700 to-accent-700 bg-clip-text text-transparent"
        >
          Helpmaton
        </h1>
      </div>
      <p class="text-display-3 mb-6 font-display text-neutral-900">
        Stop juggling AI agents. Get
        <span class="text-primary-600">organized</span>
        <span class="text-accent-600">workspaces</span> that actually work.
      </p>
      <p
        class="text-xl text-neutral-700 mb-6 max-w-2xl mx-auto leading-relaxed font-medium"
      >
        Create AI assistants that remember your conversations, connect to your
        tools, and run on a schedule. Launch in minutes with Gmail, Google
        Calendar, Notion, Slack, and Discord—no complex setup.
      </p>
      <div class="flex items-center justify-center gap-6 mb-8 flex-wrap">
        <p class="text-sm text-neutral-600">
          <span
            class="inline-flex items-center gap-2 px-3 py-1 bg-white/80 backdrop-blur-sm rounded-full border border-primary-200"
          >
            <span class="text-primary-600">🧩</span>
            <span>Source-available (BSL 1.1 → Apache 2.0 on 2029-11-01)</span>
            <a
              href="https://github.com/djinilabs/helpmaton"
              target="_blank"
              rel="noopener noreferrer"
              class="text-primary-600 hover:text-primary-700 font-semibold underline"
              >self-host when you need to</a
            >
          </span>
        </p>
      </div>
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a
          id="hero-start-free-trial"
          href="https://app.helpmaton.com"
          class="inline-block bg-gradient-to-r from-primary-600 to-primary-700 text-white px-8 py-4 text-lg font-semibold rounded-xl shadow-colored hover:shadow-large transition-all hover:scale-105 hover:from-primary-700 hover:to-primary-800"
        >
          Start Free Trial
        </a>
        <a
          id="hero-see-how-it-works"
          href="/#features"
          class="inline-block border-2 border-primary-600 text-primary-700 px-8 py-4 text-lg font-semibold rounded-xl bg-white/80 backdrop-blur-sm hover:bg-primary-50 transition-all"
        >
          See How It Works
        </a>
      </div>
    </div>
  </div>
</section>

<script>
  const startTrial = document.getElementById('hero-start-free-trial');
  const seeHow = document.getElementById('hero-see-how-it-works');

  startTrial?.addEventListener('click', (e) => {
    if (window.posthog) {
      window.posthog.capture('cta_clicked', { label: 'start_free_trial' });
    }
    const href = (e.currentTarget as HTMLAnchorElement).href;
    e.preventDefault();
    setTimeout(() => { window.location.href = href; }, 150);
  });

  seeHow?.addEventListener('click', (e) => {
    if (window.posthog) {
      window.posthog.capture('cta_clicked', { label: 'see_how_it_works' });
    }
  });
</script>
