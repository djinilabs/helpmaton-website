---
const plans = [
  {
    name: "STARTER",
    price: "$29",
    period: "/month",
    description: "For the curious and the brave. Dip your toes in the AI agent pool.",
    emoji: "🌱",
    features: [
      "1 Workspace (your first box!)",
      "5 Agents (a small robot family)",
      "100 Documents (they'll read a lot)",
      "10,000 Helpmaton API calls/month",
      "Email Support (we'll respond, promise)",
    ],
    cta: "START TRIAL",
    popular: false,
  },
  {
    name: "PRO",
    price: "$99",
    period: "/month",
    description: "For teams that mean business. Go big or go home.",
    emoji: "🚀",
    features: [
      "5 Workspaces (more boxes!)",
      "Unlimited Agents (robot army time)",
      "Unlimited Documents (knowledge is power)",
      "100,000 Helpmaton API calls/month",
      "Priority Support (we answer fast)",
      "Custom Integrations (make it yours)",
    ],
    cta: "START TRIAL",
    popular: true,
  },
  {
    name: "ENTERPRISE",
    price: "CUSTOM",
    period: "",
    description: "For the big leagues. When you need everything, everywhere, all at once.",
    emoji: "🏢",
    features: [
      "Unlimited Workspaces (all the boxes)",
      "Unlimited Agents (infinite robots)",
      "Unlimited Documents (library of Alexandria)",
      "Unlimited Helpmaton API calls (go wild)",
      "Dedicated Support (your own team)",
      "Custom Integrations (we'll build it)",
      "SLA Guarantee (we pinky swear)",
    ],
    cta: "CONTACT SALES",
    popular: false,
  },
];
---

<section id="pricing" class="bg-white p-8 py-16">
  <div class="max-w-6xl mx-auto">
    <div class="border-2 border-black p-8 mb-12 text-center">
      <h2 class="text-5xl font-bold uppercase mb-4">PRICING</h2>
      <p class="text-lg opacity-75 mb-2">
        Pick your poison. All plans include a 7-day free trial (no credit card required, we're not monsters).
      </p>
      <p class="text-sm opacity-60 italic">
        Cancel anytime. No questions asked. Well, maybe one question: "Why?"
      </p>
    </div>

    <div class="border-2 border-black p-6 mb-8 bg-accent text-white">
      <h3 class="text-2xl font-bold uppercase mb-3">💰 What's Included?</h3>
      <p class="mb-3 opacity-90">
        The prices above cover <strong>Helpmaton platform services only</strong>: hosting, agent configuration, workspaces, teams, document search, and all Helpmaton-related API calls.
      </p>
      <p class="mb-3 opacity-90">
        <strong>AI provider calls are NOT included</strong> in your subscription. For AI provider usage (like OpenAI, Anthropic, etc.), you have two options:
      </p>
      <ul class="list-disc list-inside space-y-2 text-sm opacity-90 ml-4 mb-3">
        <li>Provide your own API keys (you pay the AI provider directly)</li>
        <li>Purchase credits through Helpmaton to add to your workspace</li>
      </ul>
      <p class="text-sm opacity-75 italic">
        This keeps your platform costs predictable while giving you flexibility in how you handle AI provider costs.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {plans.map((plan) => (
        <div
          class={`border-2 p-8 ${
            plan.popular
              ? "border-accent bg-accent text-white"
              : "border-black bg-white"
          }`}
        >
          {plan.popular && (
            <div class="text-center mb-4">
              <span class="inline-block border-2 border-white bg-white text-accent px-4 py-1 font-bold uppercase text-sm">
                ⭐ MOST POPULAR ⭐
              </span>
            </div>
          )}
          <div class="flex items-center justify-center gap-2 mb-2">
            <span class="text-4xl">{plan.emoji}</span>
            <h3 class="text-3xl font-bold uppercase">{plan.name}</h3>
          </div>
          <div class="mb-4">
            <span class="text-5xl font-bold">{plan.price}</span>
            <span class="text-lg">{plan.period}</span>
          </div>
          <p class="text-sm opacity-90 mb-6">{plan.description}</p>
          <ul class="space-y-3 mb-8">
            {plan.features.map((feature) => (
              <li class="flex items-start gap-2">
                <span class="font-bold">✓</span>
                <span class="text-sm">{feature}</span>
              </li>
            ))}
          </ul>
          {plan.name === "ENTERPRISE" ? (
            <button
              data-open-sales-modal
              class={`block w-full text-center border-2 px-6 py-3 font-bold uppercase transition-colors cursor-pointer ${
                plan.popular
                  ? "border-white bg-white text-accent hover:bg-transparent hover:text-white"
                  : "border-black bg-black text-white hover:bg-accent hover:border-accent"
              }`}
            >
              {plan.cta}
            </button>
          ) : (
            <a
              href="https://app.helpmaton.com"
              class={`block text-center border-2 px-6 py-3 font-bold uppercase transition-colors ${
                plan.popular
                  ? "border-white bg-white text-accent hover:bg-transparent hover:text-white"
                  : "border-black bg-black text-white hover:bg-accent hover:border-accent"
              }`}
            >
              {plan.cta}
            </a>
          )}
        </div>
      ))}
    </div>
  </div>
</section>

